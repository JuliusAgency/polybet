/**
 * Shared TypeScript types and interfaces.
 *
 * Add application-wide types here. For component-specific types,
 * co-locate them in the component's own file.
 */

export type Nullable<T> = T | null;
