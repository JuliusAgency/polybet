// CSS design tokens — import in main.tsx:
// import '@/shared/theme/tokens.css'
// import '@/shared/theme/fonts.css'

export {
    colors,
    colorsByTheme,
    zIndex,
    radius,
    THEME_STORAGE_KEY,
    THEME_DEFAULT,
    type ThemeName,
} from './const';
